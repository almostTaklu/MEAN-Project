<!DOCTYPE html>
<html lang="en">
<head>
    <% include ./partials/head %>
</head>
<body>
    <% include ./partials/navbar %>
    <div class="container mt-5">
        <div class="form-container">
            <h1 class="form-header">Delete Blog</h1>
            <div class="delete-warning">
                <p>Are you sure you want to delete this blog? This action cannot be undone.</p>
            </div>
            <form action="/blogDelete/<%= blog._id %>" method="POST" class="form-body">
                <input type="hidden" name="_method" value="DELETE">
                <div class="form-group">
                    <label for="blogTitle">Blog Title</label>
                    <input readonly class="form-control" id="title" name="blogTitle" value="<%= blog.blogTitle %>">
                </div>
                <div class="form-group">
                    <label for="blogText">Content</label>
                    <textarea readonly class="form-control" id="entry" rows="3" name="blogEntry"><%= blog.blogText %></textarea>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-danger">Delete</button>
                    <a href="/blogList" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
    <% include ./partials/scripts %>
</body>
</html>
